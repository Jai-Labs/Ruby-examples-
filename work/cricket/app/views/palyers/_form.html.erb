<%= form_for(@palyer) do |f| %>
  <% if @palyer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@palyer.errors.count, "error") %> prohibited this palyer from being saved:</h2>
      <ul>
      <% @palyer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <div class="field">
    <%= f.label :team_name %><br>
    <%= f.select :team_id, options_from_collection_for_select(Team.all, 'id', 'name')  %>
  </div>
  


  <div class="field">
    <%= f.label :player_name %><br>
    <%= f.text_field :player_name %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.text_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
