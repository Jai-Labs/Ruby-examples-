<%= form_for(@match) do |f| %>
  <% if @match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
      <% @match.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div> 
  <% end %>
  <div class="field">
    <%= f.label :team_home %><br>
    <%= f.text_field :team_home %>
  </div>
  <div class="field">
    <%= f.label :team_away %><br>
    <%= f.text_field :team_away %>
  </div>

  <div class="field">
  <%= f.label :team_away %>
  <%= f.select :team_id, options_from_collection_for_select(Team.all, 'id' 'name') %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
